import 'package:flutter/material.dart';

class ControlareaScreen extends StatelessWidget {
  const ControlareaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
