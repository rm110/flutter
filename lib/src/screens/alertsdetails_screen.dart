import 'package:flutter/material.dart';

class AlertsdetailsScreen extends StatelessWidget {
  const AlertsdetailsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
